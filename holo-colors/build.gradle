apply plugin: 'com.android.library'

android {

	defaultConfig {
		minSdkVersion 1
	}

	buildTypes {
		release {
			minifyEnabled false
		}
	}
}

if(rootProject.name.equals("android-library")){
	apply from: '../maven.gradle'
}
